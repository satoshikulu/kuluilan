{
  "name": "kuluilan-netlify-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless functions for Kulu ilan admin operations (Supabase service role)",
  "main": "index.js",
  "scripts": {
    "build": "echo 'no build'",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0"
  },
  "devDependencies": {
    "netlify-cli": "^21.6.0"
  }
}
